tasks:
  - name: setup-aws-cli
    init: |
      cd /workspace
      curl -O https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip
      unzip awscli-exe-linux-x86_64.zip
      sudo ./aws/install --no-sign-request
      cd $THEIA_WORKSPACE_ROOT










